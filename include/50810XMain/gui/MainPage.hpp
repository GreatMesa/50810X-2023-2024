#pragma once
#include "main.h"
/* class MainPage
{
    public:
    //characteristics a page will have
    lv_obj_t* tabview = nullptr;
    lv_color_t themecolor, othercolor;
    lv_style_t pageStyle;

    //empty constructor so you can just declare variables

    /**
     * @brief Makes a new page in the gui.
     * @param parent Parent of the Page.
     * @param themecolor Theme color for page.
     * @param othercolor Text color for page.
    */
   /* MainPage(lv_obj_t* parent, lv_color_t themecolor,lv_color_t othercolor);

    ~MainPage();
    /**
     * @brief Adds a new tab.
     * @param string Name of new tab;
    */
   /*
    lv_obj_t* newTab(std::string);
};
*/